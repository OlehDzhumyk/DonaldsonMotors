meta {
  name: Log in Customer
  type: http
  seq: 2
}

post {
  url: {{baseURL}}/auth/login
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {
    "email": "joan@example.com",
    "password": "P@ssw0rd!"
  }
}

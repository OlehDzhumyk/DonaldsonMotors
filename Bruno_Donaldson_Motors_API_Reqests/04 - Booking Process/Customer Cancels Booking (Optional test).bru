meta {
  name: Customer Cancels Booking (Optional test)
  type: http
  seq: 8
}

patch {
  url: {{baseUrl}}/api/Bookings/1/cancel
  body: none
  auth: bearer
}

auth:bearer {
  token: {{customerToken}}
}

assert {
  res.status: eq 204
}
